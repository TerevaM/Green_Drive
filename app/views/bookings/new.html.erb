<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h1>Ajouter une nouvelle réservation</h1>
      <%= render 'form' %>
    </div>
    <div class="col-md-6">
      <!-- Placeholder for car image -->
      <%= image_tag @car.image_url, alt: @car.model, class: "img-fluid" if @car.image_url.present? %>

      <!-- Car details go here -->
      <h2><%= @car.brand %> <%= @car.model %></h2>
      <p><%= @car.description %></p>
      <!-- add more details as per your car model -->
    </div>
  </div>
</div>
