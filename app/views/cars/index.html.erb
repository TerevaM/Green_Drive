<%= search_form_for @q, url: cars_path, method: :get do |f| %>
  <div class="search-form-control form-group m-2">
    <%= f.label :location_cont, 'Location' %>
    <%= f.search_field :location_cont, class: "form-control string required" %>

    <%= f.label :start_date_gteq, 'Start date' %>
    <%= f.date_field :start_date_gteq, class: "form-control string required" %>

    <%= f.label :end_date_lteq, 'End date' %>
    <%= f.date_field :end_date_lteq, class: "form-control string required" %>

    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Rechercher
    </button>
  </div>
<% end %>

<br>
<%# <h1 class="h1 m-2"><%=  "Nous avons trouvé #{query.results.count} voitures électriques pour vous à {query.location} pour les {query.start_date}-#{query.end_date}"%></h1>
<br>

  <ul class="green_drive_list_cards">
<% @cars.each do |car| %>
<li class="list-group-item green_drive_card">

<!-- Slider main container -->
<div data-controller="carousel" class="swiper-container">
  <!-- Additional required wrapper -->
  <div class="swiper-wrapper">
    <!-- Slides -->
    <% car.photos.each do |photo| %>
    <div class="swiper-slide">
      <% cl_image_tag photo.key %>
    </div>
  </div>
</div>

      <% end %>
    <div class="card-body">
      <h5 class="card-title"><%= car.model %> <%= car.brand %></h5>
      <div class="card-body-text">
      <p><i class="fa-solid fa-location-dot"></i> <%= car.location %></p>
      <p class="card-text"><%= car.rate%>€/jour
      <%= link_to "Voir +", car_path(car), class: "btn btn-success" %></p>
  </div>
</li>
<% end %>
</ul>
  